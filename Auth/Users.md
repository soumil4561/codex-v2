This module will be covering the nitty gritty of user management, starting from the basics to lets see how far can this be stretched.....

### The simple users table
I think the most simplest version if there were to exist would be a standalone users table containing some basic fields: username, email, password etc.

Let's expand this along with RBAC to accomplish protection in the application

The entities:
- `users`
- `roles`
- `permissions`

if i were to expand them to a point where they overinflate themselves:
